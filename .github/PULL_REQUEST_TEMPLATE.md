<!-- One feature or fix per PR. Keep branches named like feat/token-rotation or fix/offline-copy. -->

## What does this PR change?

## Why?

## Checklist

- [ ] Builds locally (`xcodebuild -scheme ContextTransfer -configuration Release build`, or ⌘R in Xcode)
- [ ] If extraction logic changed: tested against **both** backends (Ollama local and a cloud backend)
- [ ] If user-facing behavior changed: landing page (`/website`) updated
- [ ] No secrets, keys, or local config paths committed
